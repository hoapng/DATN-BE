export enum Action {
  Manage = 'manage',
  Create = 'create',
  Read = 'read',
  Update = 'update',
  Delete = 'delete',
}

export enum Role {
  User = 'user',
  Admin = 'admin',
}

export enum TweetType {
  Post,
  Share,
  Comment,
  News,
  Review,
  Tips,
  Question,
}
